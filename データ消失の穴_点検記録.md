# データ消失の穴 — 全点検記録（2026-08-18）

> 「8/3〜8/7の作業情報が全て消えている」というご報告（実際は表示のみ・データは無事）
> の調査をきっかけに、**永続化の全経路を点検した記録**。
> 見つけた穴は塞いだが、**同じ形の穴を将来また作らないために、ここに全部残す。**

## 大前提：この構成が消失を招きやすい理由

- Render無料プランは**ディスクが揮発**する。デプロイのたびに `denki_bid.db` は作り直される。
- したがって**真の保存先は Supabase**（`kawano_kv` テーブル）。
- 各 `_push_*()` は「**今SQLiteにある全件**」で Supabase を**丸ごと上書き**する。
- つまり **SQLite が痩せた状態で1回でも書き戻すと、その差分は永久に消える。**

この「丸ごと上書き」設計自体は小さなデータなら妥当だが、
**書き戻す前に『そもそも正しく読み込めていたか』を確認していなかった**のが穴の根源。

---

## 穴の一覧（すべて修正済み・回帰テストあり）

### ① 案件が見つからない申請を、黙って捨てていた 〔最重要・実際に発動寸前〕

`restore_from_supa()` は `external_id` から現在の案件IDを引くが、
**案件データ側が壊れていると解決できず `if cid is None: continue` で捨てていた。**

発動条件と結果:
1. 案件取得が壊れ、痩せたDBが本番へ配られる（2026-08-09〜17に実際に発生）
2. 起動時の復元で申請が復元されない（画面から消えて見える）
3. 利用者が**1件でも編集**すると `_push_applications()` が走る
4. 欠けた状態で Supabase が上書きされ、**お客様の入力が永久に消える**

**上西さんは 2026-08-17 09:55 にNG判定を1件されている。**
案件データの復旧が12:15。**タイミングが少しずれていれば200件超が本当に消えていた。**

対策: `_unlinked_apps` に退避し、書き戻すとき必ず混ぜ戻す。
**案件が一時的に見つからないことは、お客様の入力を消してよい理由には決してならない。**

### ② 復元が全キー1つの try で、途中の失敗が以降を全滅させる 〔波及が大きい〕

`restore_from_supa()` は 申請→協力会社→マイ条件→監視機関の除外→NG集計→AI使用量 を
**1つの try でまとめて**処理していた。途中で1回でも例外が出ると、
**それ以降のキーが丸ごと未復元**のまま起動する。その状態で利用者が1操作すると、
空のまま上書きされて永久に失われる。

対策: `_restore_section()` でキー単位に分離。1つの失敗が他を巻き添えにしない。

### ③ 「読み込めていないのに書き戻す」を誰も止めていなかった 〔6キー全部〕

`_push_applications` / `_push_companies` / `_push_profile` / `_push_exclusions` /
`_push_ng_reports` / `_push_ai_usage` のいずれも、
**復元できていたかを確認せずに丸ごと上書き**していた。

対策: キーごとに復元結果（loaded / empty / failed）を記録し、
**failed のキーは書き戻さない**。判断に迷ったら書かない＝消えるより残す。

とくに **AI使用量は請求根拠**。消えるとお客様に請求できなくなる。

### ④ 保存を見送ったことが利用者に伝わらない 〔黙って止めるのも罪〕

危険な書き戻しを止めるだけでは、利用者は「保存できた」と思い込む。

対策: `supa.block_save(理由)` で記録し、画面バナーに**日本語で理由を出す**。
「何が保存されなかったか」が分かるようにする。

### ⑤ 一度に大きく減る書き戻しを検知していなかった 〔最後の砦〕

①②③を全部すり抜けても、件数が急に減る書き戻しは異常。

対策: 前回件数の70%未満に減る書き戻しは中止して警告。
1件ずつの削除は正常なので通す（狼少年にしない）。

### ⑥ 消えたときに戻す控えが無かった 〔復旧手段ゼロ〕

今回もし消えていたら、**戻す手段が何も無かった。**

対策: 起動時、読み込めた正常な状態を `applications_bak_YYYYMMDD` として控える。
**控えの取得に失敗しても復元は必ず続行する**（保険の失敗で本体を止めない）。

### ⑦ 対策をやり過ぎて、新規導入で一度も保存できなくなる 〔自分で作った穴〕

③を入れた結果、Supabaseが空の新規導入では「復元記録が無い」＝永久にブロック、
という状態を作りかけた。**過保護で保存不能にするのは、消えるのと同じくらい悪い。**

対策: 復元が最後まで走り切ったかを記録し、走り切った上で記録が無いキーは
「サーバに元々何も無い＝失うものが無い」とみなして許可する。

### ⑧ 入力欄はあるのに保存されない 〔別系統だが同じ「黙って消える」〕

案件詳細ページのフォームは hidden の `managed` に載っている項目しか保存しない。
欄を追加して `managed` に足し忘れると、**入力できるのに保存されない**。

対策: テンプレートを解析して取りこぼしを検出する検査を `test_applications_sync.py` に追加。
**検査自体が機能しているか（欄を1つ抜いたら気づけるか）も同じテストで担保**している。

---

## 調べるときの注意（次に誰かが調査するとき用）

- **`updated_at` は使えない。** 復元時にその時刻で上書きされるため、全件が「今日」になる。
  **お客様が実際に編集した時刻は `client_mtime`（ms epoch）**を見ること。
- 本番の申請一覧は `/applications?sheet=公共` のHTML内 `<script id="casesData">` から取れる。
- 「消えた」と言われても、まず**本当に消えているか**を数字で確認する。
  今回は表示されていないだけで、Supabaseには全件残っていた。

## 回帰テスト

`test_data_loss.py`（21件）。CI（render.yaml / .github/workflows/update.yml）に組込済み。
**修正前のコードでは実際に落ちることを確認済み**（`git show <修正前>:db.py` を
別ディレクトリに置いて実行するのが手軽）。

姉妹アプリ `NJSS_DiluNova` にも①〜⑦の同じ穴があり、同日に同じ対策を入れた
（あちらは**保存失敗を知らせるバナー自体が無かった**ので併せて新設）。
