# 現在地と続き（Kawanoさん NJSSモドキ）

> このファイルを見れば、次回ここから再開できる。最終更新の状態をまとめる。

## ★最新（2026-08-18）: 「8/3〜8/7の作業が全部消えた」の調査結果と、実在した消失経路

上西さんから「最新公告が取得できない／8/3〜8/7頃で作業していた情報も全て消えている」
とのご連絡（8/17 午前10:40・10:55＝ちょうど取得障害を直している最中）。

**結論: データは消えていなかった。** 本番の `client_mtime`（お客様が編集した時刻）で確認:

| 編集日 | 件数 |
|---|---|
| 8/03 | 7件 |
| 8/04 | 16件 |
| 8/05 | 50件 |
| 8/06 | 94件 |
| 8/07 | 41件 |
| **8/3〜8/7 計** | **208件（全て現存）** |

管理シート全325件。案件データが痩せていた（1,997件）ため紐付けが解決できず
**表示されていなかっただけ**で、Supabaseには残っていた。案件復旧後に自動で戻った。
※`updated_at` は復元時刻に上書きされるため調査に使えない。**`client_mtime` を見ること。**

### ただし「永久に消える経路」が実在した（コミット 5804209 で修正）

1. 案件(cases)の取得が壊れ、痩せたDBが本番へ配られる
2. 起動時の `restore_from_supa()` が external_id を今の案件IDに解決できず、
   **申請を黙って捨てる**（`if cid is None: continue`）
3. 利用者が**1件でも編集**すると `_push_applications()` が走る
4. これは「今SQLiteにある全件」でSupabaseを**丸ごと上書き**する実装のため、
   2で捨てられた申請が**永久に消える**

**上西さんは8/17 09:55にNG判定を1件されている。**タイミングが少しずれていれば
200件以上が本当に消えていた。今回助かったのは運。

**対策（三重）**:
- `_unlinked_apps` … 復元できなかった申請を捨てずに退避し、書き戻すとき必ず混ぜ戻す。
  **案件が一時的に見つからないことは、お客様の入力を消してよい理由にならない。**
- 件数が急減する書き戻しは `supa.block_save()` で中止し、**理由つきで画面に警告**。
  黙って止めると保存できたと思い込まれるため、必ず伝える。
- 起動時に読み込めた正常な状態を `applications_bak_YYYYMMDD` として控える。
  控えの取得に失敗しても復元は続行する（保険の失敗で本体を止めない）。

回帰テスト `test_data_loss.py`（11件）をCIに組込。**修正前のコードでは実際に落ちる
（申請が消える）ことを確認済み。** 姉妹アプリ NJSS_DiluNova にも同じ欠陥があったため
先回りで修正（あちらは保存失敗バナー自体が無かったので併せて追加）。

## ★最新（2026-08-17）: 本番データが6週間止まっていた障害を修正

画面に「データ更新が止まっている可能性があります（総件数 1,997件／最新公告 2026-08-12）」
が出ていた。本番は全国網羅で数万件入る想定なので、1,997件の時点で異常だった。

**壊れた日（ログで確定）**:
- **本番（GitHub Actions・Ubuntu）は 2026-08-09 から**。前日8/8までは 45,149件 取れていて、
  8/9の回で突然0件になった。**kkj.go.jp がサーバ証明書を更新した日（notBefore
  = 8/9 03:22 UTC）と一致**する。＝証明書更新のときに中間CAの設定を間違えたのが引き金。
- **ローカルMacの毎朝のジョブは 2026-07-07 から**（6週間）。こちらが先に壊れていたため
  気づく機会はあったが、ログに「取得0件」としか出ず見逃していた。

**原因は3つ重なっていた**（コミット `d5298d5`）:

1. **証明書エラーで官公需API(kkj.go.jp)の取得が全滅**。
   kkj.go.jp がサーバ証明書の発行元（`JPRS DV RSA CA 2024 G1`）とは**別の中間CA**
   （`JPRS Domain Validation Authority - G4`）を送る設定ミスをしている。ブラウザやcurlは
   足りない証明書を自分で取りに行くので誰も気づかないが、Pythonはそれをしないため
   `CERTIFICATE_VERIFY_FAILED` で毎回失敗していた。
   → 正しい中間CAを `certs/kkj_intermediate.pem` に**同梱**し、検証は有効なまま接続する。
     **検証を切る回避は絶対にしない**（中間者攻撃を許すため）。回帰テストで固定済み。
2. **失敗が無言だった**。`_fetch_retry` が例外を握り潰し、ログには「取得0件」としか出ず、
   6週間気づけなかった。→ 失敗理由を `last_error()` に残し、0件時にログへ必ず出す。
3. **痩せたDBが「正常」として本番へ配られた**。`--full`（GitHub Actionsの網羅生成）の
   安全弁が `--fast` と同じ下限500件だったため、1,879件でも合格して Release に公開され、
   Render がそれを取得していた。→ 下限をモード別に（`--full` は5,000件）。

**復旧の確認**: ローカル `update.py --fast` で官公需API **14,340件**（従来0件）を取得。
本番は Actions の「データ網羅更新」を手動起動して再生成 → Render 自動デプロイ。

**同時に直したもの（ローカル環境）**: macOS 26.1 更新で Homebrew の全Python（3.12/3.13/3.14）の
`pyexpat` が壊れており（システムの libexpat が古くシンボル不足）、**XMLが一切parseできず**
毎朝のlaunchdジョブは証明書を直しても動かない状態だった。`.venv` を uv 管理のPython 3.13で
作り直して解消（旧venvは `.venv.broken-expat-20260817/` に温存）。**もう1台のMacでも同じ症状が
出るはず**。出たら `uv venv --python 3.13 .venv` で作り直すこと。

**姉妹アプリ `NJSS_DiluNova` にも同じ地雷があったので同じ対策を入れた**（コミット `270d757`）。

### 二度と無言で壊れないように入れた多層防御（コミット `e9dc335` / `a711fa7`）

判定基準は **`data_expectations.py` 1ファイルに集約**した。update.py・audit.py・画面バナー・
外形監視が全部この同じ関数を呼ぶ。基準を各所にバラバラに持つと「ビルドは通ったのに画面には
警告が出る」というズレが起きるため、**絶対に分岐させないこと**。

| 層 | 何をするか |
|---|---|
| `preflight.py` | 30分の重い取得を始める前に実接続。繋がらなければ即中止 |
| `kkj_scraper` の自己修復 | 同梱CAで検証できないとき、証明書のAIAから正しい中間CAを取得して再試行。CAがまた変わっても自動復旧する |
| `update.py` の検品 | 取得元別の件数と鮮度を見て、重大なら非0終了で公開を止める |
| `data_baseline.json` | 前回正常時の件数。60%未満に減ったら重大（下限は超えている「静かな半減」を捕まえる）。**正常だった回しか更新しない** |
| `audit.py` | 同じ検品結果を毎日のレポートに載せる |
| `/api/data-health` | 同じ判定をJSONで公開。重大なら HTTP 503 |
| `.github/workflows/watchdog.yml` | 本番を毎日外から叩き、異常ならActionsを赤くする＝メールが飛ぶ |
| `run_daily.sh` | 異常時に**macOSの通知センター**に出す。※このファイルはgitignore（マシン固有のパスのため）。**もう1台のMacには手で同じものを置くこと** |

事故当時の状態（官公需APIを全削除）を注入して、各層が実際に止めることを確認済み。
回帰テスト `test_data_expectations.py`（10件）で固定してある。
**指摘されたズレは、その場で `data_expectations.py` の EXPECT に追記すること。**

## （2026-07-09）: 監視機関を全9,041機関に拡大（上西さん要望）

NJSS「発注機関を探す」の全9,041機関を取得・URL解決し、**6,886機関を監視機関に追加**
（本番反映済み・監視機関 1,000→6,891）。受付中案件ベースのカバレッジ98.2%。

- **仕組み**（すべてコミット済み）:
  - `njss_org_scraper.py` — NJSS機関一覧の取得（Playwright・レジューム式）。
    未ログインは1日数ページで閲覧上限 → **`--login` で実ブラウザを開き人がログイン**
    →セッションを `njss_session.json`（gitignore・秘密）に保存→以降は上限なし。
    `--category 114`=独法のみ / `--category ""`=全件。
  - `njss_org_resolver.py` — 公式サイトURL解決＋調達ページ自動探索＋理由分類。
    ルーティング: 確定リスト(KNOWN_BID_PAGES)→既存シート→独法親法人(PARENT_SITES)
    →都道府県庁(PREF_SITES・県警含む)→市区町村(research/localgovjp.json)
    →府省庁(MINISTRY_SITES)→大学(UNIV_SITES＋research/public_univ_sites.json)
    →特殊法人等(EXTRA_ORG_SITES)。403/406=Bot遮断は「要確認」扱い。
    **注意: 自分が取り込んだagencies行(sample_urlがNJSS機関ページ)は「既存シート」
    と誤認しないこと（自己汚染ガード実装済み・触るとき注意）**
  - `njss_org_daily.py` — 毎朝7時のlaunchd(run_daily.sh)から実行。差分取得→再解決
    →CSV再生成→ローカルcommit（pushは人）。
  - `agency_import.load_extra()` — `research/njss_dokuho_agencies.csv` をagenciesに
    マージ取り込み（スプシ既載は上書きせず・自分の行は毎回更新）。update.py両経路に組込済。
- **成果物**: `research/njss_dokuho_report.csv`（全9,041件の可否・理由・URL）、
  Excel版 `research/監視機関_全9041件_追加可否リスト.xlsx`（~/Downloads/にもコピー済み、
  **上西さんへ送付予定**）。生成は `/usr/bin/python3`（venvはpyexpat破損でopenpyxl不可）。
- **数字**: 追加済み5,890＋要確認996＝追加可6,886 / 追加不可2,155
  （閉鎖335・URL不明1,790=広域行政組合や小規模公社等・到達不可30）。
- **残タスク（次の再開ポイント）**:
  1. 上西さんへExcelと報告を送る（松本さんの操作。返信文案はセッション記録にあり）
  2. URL不明1,790件のうち受付中案件が多い機関（甲賀広域行政組合44件など）を
     人力 or 個別検索で潰す（上西さん側で人力確認する合意あり）
  3. NJSSログインセッション失効時: `.venv/bin/python njss_org_scraper.py --login`
  4. 要確認996件（調達ページ未発見）の深掘り改善（任意）

## 1. 何ができているか（現在地）

- **公開URL**: https://kawano-njss-modoki.onrender.com （Render無料プラン）
- **GitHubリポジトリ**: https://github.com/syun3032-tech/dgss （private）
- **案件データ**: 全国の電気工事入札 **約2000件**（全47都道府県・関西厚め）。すべて実データ。
- **監視機関**: 全国 **1000機関**（公式入札ページへの導線つき）
- **毎日自動更新**: GitHub Actions が毎日 `update.py --fast` を実行 → DB更新 → push → Render自動再デプロイ。**完全無料**。稼働実績あり（2026-06-11 成功）。

### 機能（NJSS相当）
案件検索（地方→都道府県）／新着／マイ条件マッチング（業種・資格 複数選択）／
**自社の競合企業**（落札者・自社除外・エリア絞り）／入札参加申請の管理／
仕様書の取得可否＋理由／監視機関一覧／**CSVダウンロード**（/export.csv）

## 2. データソース（重要）

| ソース | 実装 | 役割 | 取得方式 |
|---|---|---|---|
| **官公需情報ポータルAPI**（中小企業庁 kkj.go.jp）| `kkj_scraper.py` | **主力**。国・地方・独法を全国横断集約。仕様書添付つき | HTTP+XML（Playwright不要） |
| PPI（i-ppi.jp）| `ppi_scraper.py` | 国の機関＋**落札者=競合データ** | Playwright |
| efftis（京都府）| `kyoto_scraper.py` | 京都府の自治体 | Playwright |
| e-Aichi（愛知県）| `aichi_scraper.py` | 愛知県＋県内 | Playwright |
| PPUBC（堺市・明石市）| `ppubc_scraper.py` | 大阪/兵庫の自治体。INSTANCESにbase追加で拡張 | Playwright |
| 電子入札コアシステム（茨城）| `koukai_scraper.py` | KF00x系 | Playwright |
| 監視機関リスト | `agency_import.py` | クライアント提供スプシ(1000機関) | HTTP(CSV) |

- **スプシ版**（自動巡回・新着）: `gas/kkj_to_sheet.gs`（Google Apps Script）。スプレッドシートに貼って「初期設定」実行で、毎日 官公需API→新着追記。サーバー不要・無料。

## 3. 運用（更新の仕組み）

- **毎日自動（無料）**: GitHub Actions `.github/workflows/update.yml` が `update.py --fast`。
  - `--fast` = 官公需API＋監視機関のみ（HTTPのみ・高速・堅牢）。**官公需APIの行だけ入れ替え、PPI競合や自治体詳細は保持**。
- **手動フル更新**（PPI競合・自治体も全部取り直す）:
  ```bash
  cd kawano-njss-modoki
  python3.13 -m venv .venv && source .venv/bin/activate   # ※python3.14は環境破損
  pip install -r requirements-local.txt && python -m playwright install chromium
  python update.py --reset        # 全ソース取得（数分・Playwright使用）
  git add -A && git commit -m "..." && git push   # → Render自動再デプロイ
  ```

## 4. 次の一手（やるとさらに強くなる順）

1. **官公需「落札結果」API/データ併用** → 競合(落札者)を全国分に拡充（今はPPIのみ）。
   p-portal.go.jp に「落札実績オープンデータ」あり（research参照）。
2. **PPUBC他市の追加**：東大阪/加古川/奈良はefftisでも構造差で未対応。`ppubc_scraper`のexecLink/フォーム検出を分岐対応すれば追加可。
3. **申請管理もlocalStorage永続化**（マイ条件は対策済。同方式で申請も消えないようにできる）。
4. **官公需APIの絞り込み精緻化**：Procedure_Type / Certification(等級) / 日付範囲での絞り込み。

## 5. 既知の制約と対策状況
- Render無料：無アクセス時スリープ（次アクセス~20秒）。料金0。←仕様。
- **マイ条件の永続化＝対策済み**：localStorageに保存し、再デプロイでサーバ側が消えても次回アクセス時に自動でサーバへ復元（base.html/profile.htmlのJS、実機検証済）。
- **API取得失敗時の欠損＝対策済み**：先に取得し成功(>0件)時のみ差し替える方式（update.py）。落ちている時は既存維持。
- 申請管理（申請ステータス）はまだ揮発する（マイ条件と同じ方式でlocalStorage化すれば対策可・未対応）。
- 自治体の個別Playwrightスクレイパーはサイト構造変更で壊れ得る（try/exceptでスキップ）。官公需APIが主力（HTTP）なので日々の安定性に影響なし。

## 6. リサーチ成果物
- `research/platform_roadmap.csv` … 全国1000機関の使用システム分析（どの基盤が何機関カバーか）
- `research/kawano_njss_cases.csv` … 現在の全案件CSV
- `research/RESEARCH.md` … データ強化の方向性

### 2026-08-17 追記: 直しきりで入れた残りの層

- **案件詳細ページに質疑期間・開札日を追加**。管理シートのモーダルにしか無く、入口に
  よって編集できる項目が違っていた。あわせて **`managed` の取りこぼし検査**を
  test_applications_sync.py に追加（入力欄があるのに hidden の managed に載っていないと
  「入力できるのに保存されない」ため）。**検査自体が機能しているかも同じテストで担保**。
- **`alert.yml`**: 網羅更新／外形監視が失敗したらIssueを起票、復旧したら自動クローズ。
  追加シークレット不要（備え付けの GITHUB_TOKEN のみ）＝設定漏れで働かない層にしない。
- **`run_daily.sh` をgit管理下へ**。`${0:A:h}` で自分の位置からリポジトリを特定するので
  2台のMacで同じものが使える。venvのpyexpatが壊れていたら uv で自動的に作り直す。
- **`/api/data-health` は AUTH_REQUIRED=1 でも認証不要**（`_PUBLIC_ENDPOINTS` に登録）。
  ここを外すと外形監視が永久に赤くなるので、両アプリで実際に確認済み。
- **注意（未対応）**: このリポジトリは **public**。db.py に顧客の会社名・代表者氏名・
  担当者名・発注機関別の等級が実データで入っている。Renderが認証なしでReleaseのDBを
  取得するための設計だが、非公開化するなら取得方法の変更が必要。松本さんへ報告済み。
